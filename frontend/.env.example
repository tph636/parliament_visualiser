FRONTEND_PORT=3001
ENVIRONMENT=development
NODE_ENV=development
PORT=3001
# For client-side requests (browser). In dev, use http://localhost:3000
VITE_BACKEND_API_URL=
# For server-side loaders inside the frontend container
INTERNAL_BACKEND_API_URL=http://backend:3000
