BACKEND_PORT=3000
ENVIRONMENT=development
NODE_ENV=development

# App
PORT=3000

# Database (used by backend and postgres service)

# Postgres container init vars (for postgres service)
POSTGRES_DB=parliament
POSTGRES_USER=parliament_user
POSTGRES_PASSWORD=change_me_dev
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
